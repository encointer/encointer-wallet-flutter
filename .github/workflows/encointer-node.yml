name: Encointer Node

on:
  workflow_call:
    inputs:
      docker_tag:
        required: true
        type: string
    outputs:
      ws_endpoint:
        description: "Public WebSocket endpoint of encointer-node"
        value: ${{ jobs.node.outputs.ws_endpoint }}

jobs:
  node:
    runs-on: ubuntu-latest
    outputs:
      ws_endpoint: ${{ steps.export_tunnel.outputs.ws }}

    steps:
      - uses: actions/checkout@v4

      - name: Run encointer-node
        run: |
          ./scripts/docker_run_encointer_node_notee.sh ${{ inputs.docker_tag }} &

      - name: Bootstrap demo community
        run: |
          ./scripts/docker_run_encointer_client_bootstrap_demo_community.sh \
            --docker-tag=${{ inputs.docker_tag }}

      - name: Start Cloudflare tunnel
        run: |
          curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o cloudflared
          chmod +x cloudflared
          ./cloudflared tunnel --url http://localhost:9944 > tunnel.log 2>&1 &
          sleep 5

      - id: export_tunnel
        name: Export WS endpoint + DNS check
        run: |
          set -euo pipefail

          HTTP_URL=$(grep -o 'https://[^ ]*trycloudflare.com' tunnel.log | head -n1)

          if [ -z "$HTTP_URL" ]; then
            echo "❌ ERROR: Could not find tunnel URL in cloudflared log"
            echo "---- cloudflared log ----"
            cat tunnel.log
            exit 1
          fi

          echo "🌍 Tunnel URL: $HTTP_URL"

          # DNS + connectivity check (portable)
          echo "🔎 Verifying tunnel is reachable…"
          for i in {1..10}; do
            if curl -fsS --connect-timeout 3 "$HTTP_URL" >/dev/null; then
              echo "✅ Tunnel reachable"
              break
            fi
            echo "⏳ Waiting for tunnel..."
            sleep 3
          done

          if ! curl -fsS --connect-timeout 3 "$HTTP_URL" >/dev/null; then
            echo "❌ ERROR: Tunnel not reachable after retries"
            exit 1
          fi

          WS_URL="${HTTP_URL/https:/wss:}"
          echo "🔌 WS endpoint: $WS_URL"

          echo "ws=$WS_URL" >> "$GITHUB_OUTPUT"