/// Test tag for tests that need the encointer-node.
const encointerNodeE2E = 'encointer-node-e2e';

/// Test tag for running against a production network
const productionE2E = 'production-e2e';

/// Test tag for running against a production network
const endpointHealthE2E = 'endpoint-health-e2e';

/// Test tag for IPFS upload e2e tests against the production gateway
const ipfsUploadE2E = 'ipfs-upload-e2e';
