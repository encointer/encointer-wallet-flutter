// ignore_for_file: prefer_const_constructors
import 'package:test/test.dart';

void main() {
  group('mock test', () {
    test('test files and test has to exist otherwise packages unit test returns error', () {
      expect(true, true);
    });
  });
}
