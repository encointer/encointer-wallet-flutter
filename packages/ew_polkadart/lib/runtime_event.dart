/// Runtime event types.
///
/// Export them under the `runtime_event.dart` domain due to name conflicts with other types.
library;

export 'generated/encointer_kusama/types/encointer_node_notee_runtime/runtime_event.dart';
