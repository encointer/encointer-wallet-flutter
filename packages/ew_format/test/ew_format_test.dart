import 'package:ew_format/ew_format.dart';
import 'package:test/test.dart';

void main() {
  group('EwFormat', () {
    test('can be instantiated', () {
      expect(const EwFormat(), isNotNull);
    });
  });
}
